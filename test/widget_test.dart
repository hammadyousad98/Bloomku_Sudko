import 'package:flutter_test/flutter_test.dart';
import 'package:zendoku/app.dart';

void main() {
  test('BloomkuApp can be constructed', () {
    expect(const BloomkuApp(), isA<BloomkuApp>());
  });
}
